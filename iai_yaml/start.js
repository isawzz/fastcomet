let playerToken = null;
let currentGameId = null;
let boardState = [["", "", ""], ["", "", ""], ["", "", ""]];

onload = start;

function start() {
	//setInterval(() => getGameState(5), 5000);  // Poll every 5 seconds
	//login();
}

